{% apply inky_to_html|inline_css(source('@css/foundation-emails.scss')) %}
    <container>
        <row>
            <colums>
                <div style="background: #212529">
                    <h1 class="text-center" style="color: #FFC107"><b><i>FITNESS PARK</i></b></h1>
                </div>

                <h4 class="text-center">Gestion des permissions</h4>
            </colums>
        </row>
        <spacer size="24"></spacer>
        <row>
            <colums>
                <p>Bonjour,</p>
            </colums>
        </row>
        <spacer size="16"></spacer>
        <row>
            <colums>
                <p>Nous vous informons qu'un contrat a été créé pour les structures suivantes :</p>
            </colums>
        </row>
        <spacer size="16"></spacer>
        <row>
            <colums>
                {% for branch in clientGrants.branch %}
                    - <b>{{ client.name }} - {{ branch.name }}</b>
                {% endfor %}
            </colums>
        </row>
        <spacer size="16"></spacer>
        <row>
            <colums>
                <p>Merci de confirmer l'ouverture des droits en cliquant sur le lien ci-dessous.</p>
            </colums>
        </row>
        <spacer size="16"></spacer>
        <row>
            <colums>
                <table class="button warning">
                    <table>
                        <tr>
                            <td>
                                <a href="{{ path('app_create_perms', { 'token': token }) }}">Ouvrir les droits</a>
                            </td>
                        </tr>
                    </table>
                </table>
            </colums>
        </row>
    </container>
{% endapply %}